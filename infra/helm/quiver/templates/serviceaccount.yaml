{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "quiver.serviceAccountName" . }}
  labels:
    {{- include "quiver.labels" . | nindent 4 }}
{{- end }}
